<% if blog_entry.image? %>
 <%= link_to image_tag(blog_entry.image.url(:index), :class=>"image_left"), {:action => "show",
  :id => blog_entry.id,
  :tag => params[:tag],
  :year => params[:year],
  :month => params[:month]} %>
<% end %>

<h2 style="margin: 0px; padding: 0px; font-size: 14px;"><%= link_to h(blog_entry.headline), {:action => "show",
  :id => blog_entry.id,
  :tag => params[:tag],
  :year => params[:year],
  :month => params[:month]} %></h2>

<span class="date"><%=h simple_date_format blog_entry.date %></span>

<%=h blog_entry.summary %><br />

<p align="right" class="more"><%= link_to "Read more", {:action => "show",
  :id => blog_entry.id,
  :tag => params[:tag],
  :year => params[:year],
  :month => params[:month]}, {:class=>"small"} %></p>

  <hr />
